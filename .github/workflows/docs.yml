name: Docs

on:
  push:
    branches: [main]
  pull_request:
    paths:
      - "docs/**"
      - "mkdocs.yml"
  workflow_dispatch:

# Défaut minimal (lecture seule) : le job build n'a besoin de rien de plus, y
# compris quand il tourne sur une PR. `pages`/`id-token` sont réservés au job
# deploy (pas de permission Pages ni de lock `pages` depuis une PR).
permissions:
  contents: read

# Un seul build en vol par ref (PR ou main) : dédiée au job build, distincte du
# lock `pages` du déploiement (job deploy, plus bas).
concurrency:
  group: docs-build-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: build (mkdocs --strict)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Set up uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
          python-version: "3.13"

      - name: Install docs dependencies
        run: uv sync --locked --group docs

      # --strict sur PR (garde-fou anti-pourrissement) ET sur main : un lien
      # interne mort ou une page hors nav casse la CI, où qu'il soit introduit.
      - name: Build site (strict)
        run: uv run --group docs mkdocs build --strict

      - name: Upload Pages artifact
        if: github.event_name != 'pull_request'
        uses: actions/upload-pages-artifact@v5
        with:
          path: site/

  deploy:
    name: deploy (pages)
    needs: build
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    concurrency:
      group: pages
      cancel-in-progress: true
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
